---
- hosts: localhost
  remote_user: root
  vars:
    - mysql_databases:
        - name: redmine
    - mysql_users:
        - name: redmine
          password: password
          priv: "redmine.*:ALL"
    - redmine_configure_firewalld: false
  roles:
    - role: geerlingguy.mysql
      become: true
    - role: role_under_test
